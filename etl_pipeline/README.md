# ETL Pipeline

## Quick start:

### 1. Execute via Python

** Just go to etl_pipiline and run: python etl_pipeline.py **

### 2.Execute using docker if you have docker installed and not python installed:

docker-compose up or docker compose up depending in the version